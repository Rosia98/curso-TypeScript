/**
 * START: Sigue las instrucciones a continuación.
 */

// Crea un alias de tipo `Country` para el tipo `string`.
// Cambia el tipo para cada variable para usar el tipo `Country`.

type Country = string; // Creación del alias de tipo

const country1: Country = "Nigeria"; // Uso del alias de tipo `Country`
const country2: Country = "Italy";   // Uso del alias de tipo `Country`
const country3: Country = "China";   // Uso del alias de tipo `Country`

// ----

export {};

/**
 * En este ejercicio, se crea un alias de tipo llamado Country que representa el tipo string. Luego, se cambia el tipo de cada variable 
 * para utilizar este nuevo alias de tipo Country, en lugar de string.

Esto proporciona una mayor claridad y abstracción en el código, ya que ahora cuando se ve el tipo Country, se entiende que se refiere a 
una cadena que representa un país.
 */